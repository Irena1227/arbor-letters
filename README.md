# Arbor Letters

A private love-letter archive for Avenil, built as a static GitHub Pages site.

Open `index.html` directly or publish the repository with GitHub Pages from the `main` branch and `/ (root)`.
